1. Написати форму для створення нотаток

2. Написати вікно (view?)с деталями нотатки і можливістю редагування її деталей:

Назви title 

Тексту text 

Нагадувань reminder 

Категорії category 

3. Додати до попереднього вікна view ? можливість видалення нотатки

в джанго shell видаленння 

from notes.models import Note

python manage.py shell

note2 = Note.objects.get(title='Note2')
note2.delete()


